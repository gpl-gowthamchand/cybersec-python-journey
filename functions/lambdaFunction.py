# Lambda Functions 
''' lambda function is short, anonymous function defined using lambda keyword.
It can take any number of arguments but restricted to a single expression, the result of which 
is automatically returned. 

var_name = lambda arg : expression 

'''

cube_of_num = lambda number : number**3 
print(cube_of_num(3))